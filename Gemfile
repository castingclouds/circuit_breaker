source 'https://rubygems.org'

gem 'concurrent-ruby', '~> 1.2'  # For concurrent operations
gem 'nats-pure', '~> 2.4'
gem 'json', '~> 2.6'  # For JSON parsing
gem 'securerandom'
gem 'json-schema'  # For JSON schema validation
gem 'redcarpet'    # For Markdown rendering
gem 'nokogiri'     # For XML processing
gem 'async'        # For async operations

group :development do
  gem 'rspec', '~> 3.12'  # For testing
  gem 'pry'
end
